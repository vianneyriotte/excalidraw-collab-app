# Workflow Status - BMad Method
# This tracks progress through BMM methodology Analysis, Planning, and Solutioning phases.
# Implementation phase is tracked separately in sprint-status.yaml

# STATUS DEFINITIONS:
# ==================
# Initial Status (before completion):
#   - required: Must be completed to progress
#   - optional: Can be completed but not required
#   - recommended: Strongly suggested but not required
#   - conditional: Required only if certain conditions met (e.g., if_has_ui)
#
# Completion Status:
#   - {file-path}: File created/found (e.g., "docs/product-brief.md")
#   - skipped: Optional/conditional workflow that was skipped

generated: "2026-01-08"
project: "excalidraw-collab-app"
project_type: "web-application"
selected_track: "bmad-method"
field_type: "brownfield"
workflow_path: "method-brownfield.yaml"

workflow_status:
  # Phase 0 - Documentation (Prerequisite for brownfield)
  - phase: 0
    name: "Documentation"
    note: "Prerequisite for brownfield - understand existing codebase"
    workflows:
      - id: "document-project"
        status: required
        agent: "analyst"
        command: "/bmad:bmm:workflows:document-project"
        purpose: "Analyze and document existing codebase"

  # Phase 1 - Analysis (Optional - user declined)
  - phase: 1
    name: "Analysis"
    note: "Optional discovery workflows - skipped by user choice"
    workflows:
      - id: "brainstorm-project"
        status: skipped
        agent: "analyst"
        command: "/bmad:bmm:workflows:brainstorming"
        note: "Skipped - user chose no discovery workflows"
      - id: "research"
        status: skipped
        agent: "analyst"
        command: "/bmad:bmm:workflows:research"
        note: "Skipped - user chose no discovery workflows"

  # Phase 2 - Planning
  - phase: 2
    name: "Planning"
    workflows:
      - id: "prd"
        status: required
        agent: "pm"
        command: "/bmad:bmm:workflows:create-prd"
        output: "Product Requirements Document with FRs and NFRs"
      - id: "create-ux-design"
        status: conditional
        condition: "if_has_ui"
        agent: "ux-designer"
        command: "/bmad:bmm:workflows:create-ux-design"
        note: "Determined after PRD - user/agent decides if needed"

  # Phase 3 - Solutioning
  - phase: 3
    name: "Solutioning"
    workflows:
      - id: "create-architecture"
        status: required
        agent: "architect"
        command: "/bmad:bmm:workflows:create-architecture"
        output: "System architecture document"
      - id: "create-epics-and-stories"
        status: required
        agent: "pm"
        command: "/bmad:bmm:workflows:create-epics-and-stories"
        note: "Break down PRD into implementable epics and stories"
      - id: "test-design"
        status: optional
        agent: "tea"
        command: "/bmad:bmm:workflows:test-design"
        output: "System-level testability review"
      - id: "implementation-readiness"
        status: required
        agent: "architect"
        command: "/bmad:bmm:workflows:implementation-readiness"
        note: "Validates PRD + Architecture + Epics + UX"

  # Phase 4 - Implementation
  - phase: 4
    name: "Implementation"
    workflows:
      - id: "sprint-planning"
        status: required
        agent: "sm"
        command: "/bmad:bmm:workflows:sprint-planning"
        note: "Creates sprint plan - subsequent work tracked in sprint-status.yaml"
